
""" Tash Me with a Twirl
 
Update your Tash Me Click program ( copy your old program here )
so the moustache will twirl when you click on it. 

Hint: See 08a_More Turtle Programs, section 'Click on the Turtle'
"""

import turtle                           # Tell Python we want to work with the turtle
turtle.setup (width=600, height=600)    # Set the size of the window

a = turtle.Turtle()                  # Create a turtle named tina

a.shape('turtle')                    # Set the shape of the turtle to a turtle
a.speed(1) 
import turtle                           # Tell Python we want to work with the turtle
turtle.setup (width=600, height=600)    # Set the size of the window


b = turtle.Turtle()                  # Create a turtle named tina

b.shape('turtle')                    # Set the shape of the turtle to a turtle
b.speed(1)  
import turtle                           # Tell Python we want to work with the turtle
turtle.setup (width=600, height=600)    # Set the size of the window


c= turtle.Turtle()                  # Create a turtle named tina

c.shape('turtle')                    # Set the shape of the turtle to a turtle
c.speed(1)
import turtle                           # Tell Python we want to work with the turtle
turtle.setup (width=600, height=600)    # Set the size of the window


d = turtle.Turtle()                  # Create a turtle named tina

b.shape('turtle')                    # Set the shape of the turtle to a turtle
d.speed(1)  
a.forward(70) 
b.forward(80)  
c.forward(90)
d.forward(100)

turtle.exitonclick()                    # Close the window when we click on it                                 


